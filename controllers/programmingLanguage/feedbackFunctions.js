export const message = (message, person, venue) => {
  return;
};

export const includeInSummary = (message, person, venue) => {
  return;
};

export const selectFollowUpInteraction = () => {
  return; 
};